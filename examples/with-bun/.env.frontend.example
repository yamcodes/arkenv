# Bun Environment Variables Examples

# Backend variables (accessible in server code)
HOST=localhost
PORT=3000
NODE_ENV=development

# Frontend variables (accessible in Bun frontend builds)
# These are prefixed with BUN_PUBLIC_ to be exposed to frontend code
BUN_PUBLIC_API_URL=https://api.example.com
BUN_PUBLIC_PORT=8080
BUN_PUBLIC_NODE_ENV=development
BUN_PUBLIC_DEBUG=true