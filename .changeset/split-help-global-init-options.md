---
"@arkenv/cli": patch
---

#### Split `--help` options into Global and `init` sections

`--help` now lists shared flags under **Global options** and scaffolding flags under **init options**, matching the multi-command `/docs/cli` taxonomy.
