---
"@arkenv/cli": minor
---

#### Auto-detect `.env.example` keys during init and suggest schema

- Robust parsing of `.env.example` to extract variable keys
- Integration with the `init` wizard to suggest keys for scaffolding
- Cleaner scaffolding templates without introductory comments
- Updated CLI documentation with new features and command steps
- Added post-scaffold guidance to refine types when using detected keys
