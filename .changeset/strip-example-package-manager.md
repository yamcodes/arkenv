---
"@arkenv/cli": patch
---

#### Strip example `packageManager` fields during scaffolding

Remove copied example `packageManager` fields during new project scaffolding so installs use the package manager selected by the CLI.
