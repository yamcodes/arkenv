---
"@arkenv/fumadocs-ui": patch
---

#### Square off the search dialog overlay

Match the docs search dialog radius to the navbar search control (`--radius-control`, 0.375rem) so the collapsed overlay is no longer a pill.
