---
"@arkenv/fumadocs-ui": patch
---

#### Tighten heading hash scroll under Site Nav

Park clicked headings at `--fd-nav-height` plus a half-rem gap instead of a fixed 8rem, so the title sits just below the floating bar instead of with a large empty band.
