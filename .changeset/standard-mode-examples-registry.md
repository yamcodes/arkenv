---
"arkenv": minor
---

#### Add `with-nextjs-standard` and `with-vite-react-standard` to example registry and scaffold defaults

Register the new Standard Schema integration examples for Next.js (`with-nextjs-standard`) and React + Vite (`with-vite-react-standard`) in the CLI example registry and scaffold defaults map.

- Add `with-nextjs-standard` and `with-vite-react-standard` entries to the bundled fallback example registry.
- Add default environment variables for `with-nextjs-standard` and `with-vite-react-standard` to the scaffold defaults map.
