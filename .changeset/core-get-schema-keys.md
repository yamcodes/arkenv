---
"arkenv": patch
---

Centralize the schema key extraction utility (`getSchemaKeys`) in the core `arkenv` package and update the `RuntimeEnvironment` type to support non-string runtime config values.
