---
"@arkenv/fumadocs-ui": patch
---

#### Align Copy page menu rows on a three-column grid

Lay each desktop actions row out as icon, label, and trailing columns with CSS grid so `<button>` rows stack with the links. **Copy page** still reserves the external-link gutter. Search-dialog width no longer stretches this popover. Round the menu panel so it is not flattened by the global Radix dialog radius.
