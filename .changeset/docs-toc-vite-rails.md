---
"@arkenv/fumadocs-ui": patch
---

#### Sit the TOC spy on the article rail

Drop the inner TOC hairline and draw the active heading marker on the column's start border (Vite `left: -1px`), so the ink bar rides the article↔TOC splitter.
