---
"@arkenv/core": patch
---

#### Show the minified gzip size of `@arkenv/core` on the README

The README size badge now reports the esbuild minified gzip size of `@arkenv/core` with `arktype` external (about 2.9 kB). ArkType stays a peer, so it is not counted in that number.
