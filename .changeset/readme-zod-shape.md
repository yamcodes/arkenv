---
"@arkenv/core": patch
---

#### Reshape the README to match the Zod page outline

The published `@arkenv/core` README now follows Zod's section order: a centered header, a badge row, a short link row, then What is ArkEnv, Features, and a single `npx arkenv init`. Validator examples stay collapsible. The RC note is a centered sentence with links to the release notes and the migration guide.
