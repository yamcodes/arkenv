/**
 * Empty client env used when `#arkenv/client-env` is imported outside strict
 * layout (so the server entry can keep a static import without Vite failing to
 * resolve the virtual specifier).
 */
export const env = {};
