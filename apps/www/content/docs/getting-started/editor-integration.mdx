---
title: Editor integration
description: Making the most of your editor with ArkEnv.
---

To get the best experience with `arkenv()`, ArkEnv provides a few utilities
for integrating with your editor.

## Syntax highlighting and inline type errors

The [ArkType VS Code extension](https://marketplace.visualstudio.com/items?itemName=arktypeio.arkdark)
and [JetBrains plugin](https://plugins.jetbrains.com/plugin/27099-arktype)
highlight ArkType schema strings and surface inline type errors.

<Callout type="info">
  Skip this section and [ArkThemes](#arkthemes) if you validate with
  [`@arkenv/standard`](/docs/reference/standard). Those schemas are Zod or
  Valibot, so the ArkType editor tools do not apply.
</Callout>

The extension highlights calls whose name contains `ark`. Keep the default
import name `arkenv`:

```diff title="./env.ts"
- import createEnv from "@arkenv/core";
+ import arkenv from "@arkenv/core";

- export const env = createEnv({
+ export const env = arkenv({
    PORT: "number.port = 3000",
    DEBUG: "boolean = false",
  });
```

## ArkThemes

ArkThemes is a VS Code (and Cursor) color theme that makes ArkType schema
strings read like TypeScript, in light and dark modes.

Visit the
[VS Code Marketplace](https://marketplace.visualstudio.com/items?itemName=arktypeio.arkthemes)
to install.

## Autocomplete on `env.*`

TypeScript infers keys and types from the schema you pass to `arkenv()`.
Import `{ env }` in application code. `env.PORT` is a `number`.
`process.env.PORT` stays an untyped string.

```ts title="./server.ts" twoslash
// @filename: env.ts
import arkenv from "@arkenv/core";

export const env = arkenv({
  PORT: "number.port = 3000",
});

// @filename: server.ts
// ---cut---
import { env } from "./env";

console.log(`Server is running on port ${env.PORT}`);
```

## Framework type augmentation

Vite and Bun can also autocomplete globals such as `import.meta.env`. The
ArkEnv CLI scaffolds `vite-env.d.ts` or `bun-env.d.ts` for those hosts.
Next.js uses `withArkEnv` codegen (`env.gen.ts`) instead.

See the [Vite](/docs/guides/frameworks/vite) and
[Bun](/docs/guides/frameworks/bun) guides, or
[Client vs. server](/docs/validating-your-environment/client-vs-server)
for Next.js.
