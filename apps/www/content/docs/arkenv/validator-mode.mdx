---
title: Validator mode
description: Configure how ArkEnv validates and coerces your environment variables.
icon: New
---

ArkEnv supports two validator modes that control how environment variables are parsed and validated.  
You select the mode via the `validator` option in the `arkenv` configuration.

## Cheatsheet - when to use which?

> If you're starting a new project, use the default `arktype` mode. 
> `standard` exists for migrations and ArkType-free setups.

| Feature | `arktype` (default) | `standard` |
| :--- | :--- | :--- |
| **ArkType DSL** | ✅ Native | ❌ Not supported |
| **Standard Schema** (Zod, Valibot, Typia) | ✅ Yes | ✅ Yes |
| **Automatic coercion** | ✅ Yes | ❌ Validator-defined |
| **Requires `arktype`** | ✅ Yes | ❌ No |
| **Best for** | New projects, maximal DX | Migrations, zero-dep setups |

---

## `validator: "arktype"` (default)

This is the default mode. ArkEnv runs validation through [ArkType](https://arktype.io) and expects it to be installed.

### What you get

- ArkType’s string-based DSL (`"string.url"`, `"number > 0"`, etc.)
- Built-in, intuitive coercion from strings
- Full Standard Schema support (Zod, Valibot, etc.)

```ts twoslash
import arkenv from 'arkenv';
import { type } from 'arkenv/arktype';
import { z } from 'zod';

const env = arkenv({
  PORT: "number.port",
  NODE_ENV: type("'development' | 'production'"),
  DATABASE_URL: z.string().url(),
});
```

### Requirements
You must install `arktype` alongside `arkenv`.

---

## `validator: "standard"`

In Standard mode, ArkEnv works directly with any validator that implements the [Standard Schema](https://standardschema.dev) interface, without ArkType.

### When this makes sense 
- You already use Zod or Valibot everywhere
- You want a zero-ArkType runtime
- You care more about minimal dependencies than DSL ergonomics

### Tradeoffs

- No ArkType DSL strings (`"number.port"`, `"string.host"`)
- No ArkType coercion - use your validator's native coercion instead

```ts twoslash
import arkenv from 'arkenv';
import { z } from 'zod';

const env = arkenv(
  {
    PORT: z.coerce.number().int().min(0).max(65535),
    DATABASE_URL: z.string().url(),
  },
  { validator: "standard" }
);
```

### Requirements
A validator that implements Standard Schema (e.g. `zod@^3.24.0`, `valibot@^1.0.0`).
