---
title: Validation
description: When and how ArkEnv checks environment variables against your schema.
---

ArkEnv's job is to prove your process has the configuration it claims to need before application code runs. Validation is that proof.

## Fail fast

Calling `arkenv()` parses the configured source immediately. Missing required keys or values that fail the schema throw [`ArkEnvError`](/docs/validating-environment-variables/error-reporting) at the call site.

```ts title="./env.ts" twoslash
import arkenv from "@arkenv/core";

export const env = arkenv({
  DATABASE_URL: "string",
  PORT: "number.port = 3000",
});
```

Importing `./env` in a server entrypoint is enough to abort a bad deploy before a request is served.

## When checks run

| Moment               | What runs                                                                               |
| -------------------- | --------------------------------------------------------------------------------------- |
| Module load          | `arkenv()` validates the current `env` source (default `process.env`)                   |
| Next.js / Nuxt build | Framework wrappers validate again during config or module setup                         |
| Vite / Bun build     | Plugins validate in the bundler pipeline                                                |
| Client bundle        | Public keys are already validated at build time; server secrets are stripped or guarded |

Framework adapters default to validating in builds. Soft-failing a production build with unchecked config is a last resort, not the default.

## Schema libraries

ArkEnv does not invent its own validator. It orchestrates a schema you provide:

| Package                                        | You supply                                                                                    | ArkEnv adds                                                                                              |
| ---------------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| [`@arkenv/core`](/docs/reference/core)         | ArkType DSL strings or `type()` definitions                                                   | Keywords (`string.host`, `number.port`), [coercion](/docs/core-concepts/coercion), env-oriented defaults |
| [`@arkenv/standard`](/docs/reference/standard) | A map of [Standard Schema](https://standardschema.dev/) validators (Zod, Valibot, and others) | The same runtime pipeline without an `arktype` peer                                                      |

Validation always ends in the schema library's rules. ArkEnv loads the input record, optionally coerces strings, then hands the result to ArkType or your Standard Schema validator. See [Standard Schema](/docs/core-concepts/standard-schema) for the ArkType-free path.

## Schema is the contract

Only keys you declare appear on the returned object (`onUndeclaredKey` defaults to `"delete"`). That keeps `env` a closed set of known fields instead of a bag of leftover process variables.

See [options](/docs/reference/options) for `safe`, `emptyAsUndefined`, and undeclared-key behavior.

## Errors carry structure

Thrown errors expose `issues` with codes such as `MISSING_VARIABLE` and `INVALID_TYPE`. Sensitive values are redacted unless you opt into debug mode. Details live under [Error reporting](/docs/validating-environment-variables/error-reporting).

## Related

- [Coercion](/docs/core-concepts/coercion) runs before the schema sees the input
- [Typesafety](/docs/core-concepts/typesafety) turns a passing schema into TypeScript types
- [Structuring your schema](/docs/validating-environment-variables/structuring-your-schema) (step-by-step guide)
- [validate](/docs/reference/validate) for framework wiring notes
