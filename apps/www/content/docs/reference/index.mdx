---
title: API reference
description: API reference for ArkEnv configuration, CLI, and packages.
---

ArkEnv’s API reference is broken up into the following sections:

## Configuration

Schema options, keywords, and environment loading behavior.

## Commands

CLI entrypoints for init, validate, and codegen.

## Packages

Published packages: `arkenv` (CLI), `@arkenv/core`, `@arkenv/standard`, and framework plugins.
