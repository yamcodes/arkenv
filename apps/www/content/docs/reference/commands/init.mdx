---
title: arkenv init
description: Scaffold ArkEnv in a new or existing project.
---

```bash
pnpm dlx arkenv@latest init
```

Stub reference for project initialization, layout selection (`--strict` / `--simple`), and agent mode (`--agent`).

> Content TBD — structure mirrors Turborepo’s `turbo` command pages.
