---
title: Security model
icon: ShieldCheck
description: Understand exactly what @arkenv/nextjs protects, what Next.js protects, and how each layout affects your leak surface.
---

The most important thing to understand about ArkEnv: **Next.js, not ArkEnv, protects your secret values.**

Any variable without a `NEXT_PUBLIC_` prefix is strictly removed from the client bundle by Next.js at build time. That happens whether you use ArkEnv or not.

So, what does ArkEnv actually do?

- **Validation** — we fail the build immediately if a required secret is missing or malformed. No more silent `undefined` errors surfacing as confusing bugs in production.
- **Type safety** — `env.PORT` is guaranteed to be a `number`, not `string | undefined`.
- **Misuse guards** — if you accidentally try to read a server-only variable in a Client Component, our runtime proxy throws a loud, descriptive error.

:::tip[Mental model]
Next.js hides your **values**. ArkEnv validates your **infrastructure** and prevents runtime misuse.
:::

## Leak surface

If you have a server-only secret like `DATABASE_URL`, here is what is actually exposed based on the layout you choose:

| Layout                 | Secret **value** in client bundle | Variable **name + type** in client bundle | Compile-time block | Runtime block (Proxy) |
| :--------------------- | :-------------------------------: | :---------------------------------------: | :----------------: | :-------------------: |
| **Flat** (recommended) |                 No                |                  **Yes**                  |         No         |        **Yes**        |
| **Strict** (split)     |                 No                |                     No                    |       **Yes**      |        **Yes**        |

Key takeaways:

- **Values are never leaked.** That is the Next.js native guarantee.
- **Flat mode exposes variable names and types** (but not values) because the single schema file is imported into your client code. For 99% of applications, knowing a variable is named `DATABASE_URL` is not a security risk.
- If your variable names reveal sensitive infrastructure (e.g. internal hostnames or vendor names), you should use the [Strict Layout](/docs/nextjs/layouts/strict). Because it uses separate files, the server schema is never imported into the client graph.

## Why Flat mode relies on runtime errors

You might wonder why Flat mode allows `env.DATABASE_URL` to autocomplete in a Client Component, only to throw an error when you actually run the code.

This comes down to how TypeScript compiles your `env.ts` file. TypeScript reads the file in a single pass. Because `env.ts` is just one file, TypeScript cannot magically return a "server type" when imported into a Server Component and a "client type" when imported into a Client Component.

If we tried to force the types to omit server keys for the browser, we would end up breaking autocomplete for your Server Components entirely.

To give you the best Developer Experience — a single file with perfect autocomplete — we intentionally type the full schema everywhere. We then rely entirely on the runtime proxy to act as the security boundary.

:::note
This approach exactly mirrors single-file mode in [`@t3-oss/env-nextjs`](https://env.t3.gg) (full type inference plus a runtime proxy throw). If you require strict compile-time boundaries, both libraries recommend splitting your configuration into separate files.
:::

## How the boundary is enforced

ArkEnv does not rely on a naive `typeof window` check to determine if code is running on the client. We use Next.js conditional package exports:

| Export condition | Next.js context                    | Accessible variables                   |
| :--------------- | :--------------------------------- | :------------------------------------- |
| `react-server`   | Server Components & Route Handlers | All variables (server, client, shared) |
| `default`        | Client Components (SSR & Browser)  | Public variables only (client, shared) |

Because this boundary is fixed during the bundling process, if a Client Component attempts to read a server-only variable, it will throw an error during SSR rendering on the server — long before that broken component ever reaches your user's browser.
