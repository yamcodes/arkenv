---
title: Introduction
icon: Album
description: Validate and secure your Next.js environment variables with zero-boilerplate scaffolding and a seamless developer experience.
---

:::important
This integration requires [ArkType](https://arktype.io) to be installed.
:::

The `@arkenv/nextjs` integration provides end-to-end environment validation for both the server and the browser. It catches configuration bugs early in your build pipeline and prevents sensitive server-side variables from ever reaching client components.

## Quickstart

The fastest way to get started is with the [ArkEnv CLI](/docs/cli). It automatically configures `@arkenv/nextjs` for your existing Next.js project.

<Tabs items={['npm', 'pnpm', 'yarn', 'bun']}>
  <Tab value="npm">
    ```bash
    npx @arkenv/cli@latest init
    ```
  </Tab>

  <Tab value="pnpm">
    ```bash
    pnpm dlx @arkenv/cli@latest init
    ```
  </Tab>

  <Tab value="yarn">
    ```bash
    yarn dlx @arkenv/cli@latest init
    ```
  </Tab>

  <Tab value="bun">
    ```bash
    bunx @arkenv/cli@latest init
    ```
  </Tab>
</Tabs>

---

## Layout strategies

ArkEnv supports two layout structures to match your security and developer experience preferences:

<Cards>
  <Card 
    title="Simple layout (recommended)" 
    href="/docs/nextjs/simple-layout"
  >
    Defines both client and server schemas in a single file (`src/env.ts`). Fully features a runtime proxy to prevent server secrets from leaking to the client. Zero-boilerplate code generation.
  </Card>
  <Card 
    title="Strict layout" 
    href="/docs/nextjs/strict-layout"
  >
    Defines client and server schemas in separate files (`env/client.ts`, `env/server.ts`, `env/internal/shared.ts`). Compile-time locked server secrets to prevent browser bundling.
  </Card>
</Cards>

---

## Custom validators

ArkEnv uses ArkType by default, but also supports any Standard Schema compatible library (like Zod or Valibot). See the [Custom validators](/docs/nextjs/using-other-validators) guide for code examples.

---

## The danger of shared variables

:::warning
Restrict the `shared` block only to `NODE_ENV`. Avoid placing custom variables (like `PORT` or other custom configuration) in the `shared` block.
:::

### The undefined fallback bug

Next.js statically strips `process.env` references from client-side bundles unless they are prefixed with `NEXT_PUBLIC_`.
If you define a custom variable in `shared` with a default value (e.g., `PORT` defaulting to `3000` or `THEME` defaulting to `'dark'`), the environment behaves asymmetrically:

- On the **server**, ArkEnv reads the actual value from the environment (e.g. `PORT = 8080`).
- On the **client**, Next.js strips `process.env.PORT` to `undefined`, causing ArkEnv to fall back to the default value (`3000`).

This asymmetry causes React hydration mismatches and corrupts client-side state. Always place non-prefixed variables under `server` so they are strictly server-only.
