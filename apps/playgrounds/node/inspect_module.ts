import { type, scope } from "arktype";

console.log("Keys of type.keywords.number:", Object.keys(type.keywords.number));
console.log("Is type.keywords.number callable?", typeof type.keywords.number);
