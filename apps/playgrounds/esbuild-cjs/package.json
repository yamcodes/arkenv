{
	"name": "@playgrounds/esbuild-cjs",
	"private": true,
	"scripts": {
		"build": "esbuild --bundle --format=cjs --platform=node --outfile=dist/index.cjs index.ts",
		"start": "node dist/index.cjs",
		"test": "pnpm build && pnpm start"
	},
	"dependencies": {
		"arkenv": "workspace:*",
		"arktype": "catalog:"
	},
	"devDependencies": {
		"esbuild": "catalog:",
		"typescript": "catalog:"
	}
}
