{
	"name": "@playgrounds/esbuild-cjs",
	"private": true,
	"scripts": {
		"build": "nubx esbuild --bundle --format=cjs --platform=node --outfile=dist/index.cjs index.ts",
		"start": "nub --node dist/index.cjs",
		"test": "nub run build && nub run start"
	},
	"dependencies": {
		"@arkenv/core": "workspace:*",
		"arktype": "catalog:"
	},
	"devDependencies": {
		"esbuild": "catalog:",
		"typescript": "catalog:"
	}
}
