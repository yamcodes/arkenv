{
	"name": "standard-schema-playground",
	"private": true,
	"type": "module",
	"scripts": {
		"start": "tsx --env-file .env index.ts",
		"dev": "tsx watch --env-file .env index.ts",
		"dev:example": "tsx watch --env-file .env.example index.ts",
		"build": "tsc",
		"clean": "rimraf dist node_modules"
	},
	"dependencies": {
		"arkenv": "workspace:*",
		"arktype": "catalog:",
		"zod": "catalog:"
	},
	"devDependencies": {
		"tsx": "catalog:",
		"typescript": "catalog:",
		"rimraf": "catalog:"
	},
	"arkenvExamples": [
		{
			"name": "with-standard-schema",
			"packageManager": "pnpm"
		}
	]
}
