# Copy to `.env` before running `pnpm dash`.
# HOST and PORT are optional; ArkEnv defaults them to 127.0.0.1:5001.

# Fine-grained token with Contents + Metadata (+ Actions if you want
# authenticated workflow widgets). Create one at:
# https://github.com/settings/tokens
# GITHUB_TOKEN=
